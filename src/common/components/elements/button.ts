import styled from "styled-components"

import { ButtonProps } from "../../types/ui-elements/button"


export const Button = styled.button<ButtonProps> `
  background-color: transparent;
  border: none;
  outline: none

`